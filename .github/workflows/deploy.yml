name: Deploy Site Sakura (manual only)

on:
  workflow_dispatch:

jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Automatic deploy is disabled. GitHub Pages should use Deploy from branch (main/docs)."
